% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer_ga.R
\name{.optimize_stv_ga}
\alias{.optimize_stv_ga}
\title{Genetic Algorithm Optimiser for ST Variogram Fitting}
\usage{
.optimize_stv_ga(
  stva_emp,
  model_template,
  bounds,
  popSize = 50L,
  maxiter = 200L,
  run = 30L,
  seed = 42L,
  lbfgsb_refine = TRUE,
  lbfgsb_maxit = 500L
)
}
\arguments{
\item{stva_emp}{Empirical ST variogram (\code{StVariogram}).}

\item{model_template}{Initial \code{vgmST} model (defines type/structure).}

\item{bounds}{List with \code{lower} and \code{upper} named numeric vectors
(from \code{\link{st_param_bounds}}).}

\item{popSize}{Integer. Population size.  Default 50.}

\item{maxiter}{Integer. Maximum number of generations.  Default 200.}

\item{run}{Integer. Stop after this many generations without improvement.
Default 30.}

\item{seed}{Integer. Random seed for reproducibility.  Default 42.}

\item{lbfgsb_refine}{Logical. Refine the GA solution with L-BFGS-B.
Default \code{TRUE}.}

\item{lbfgsb_maxit}{Integer. Maximum L-BFGS-B iterations for refinement.
Default 500.}
}
\value{
Best-fitting \code{vgmST} object found.
}
\description{
Uses the \pkg{GA} package's real-valued genetic algorithm to globally
search the WLS objective surface, then refines the solution with a short
L-BFGS-B run.  Falls back to a built-in Differential Evolution
(\code{DE/rand/1/bin}) implementation when \pkg{GA} is not installed.
}
\seealso{
\code{\link{autofitVariogramST}}, \code{\link{st_param_bounds}}
}
\keyword{internal}
