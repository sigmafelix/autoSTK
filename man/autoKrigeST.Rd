% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoKrigeST.R
\name{autoKrigeST}
\alias{autoKrigeST}
\title{Automatic Spatio-Temporal Kriging}
\usage{
autoKrigeST(
  formula,
  input_data,
  new_data,
  type_stv = "sumMetric",
  block = 0,
  model = c("Sph", "Exp", "Gau", "Ste"),
  kappa = c(0.05, seq(0.2, 2, 0.1), 5, 10),
  fix.values = c(NA, NA, NA),
  newdata_mode = "rect",
  newdata_npoints = 3000,
  GLS.model = NA,
  tlags = 0:6,
  cutoff = 20000,
  width = 500,
  forward = 6,
  predict_chunk = NULL,
  nmax = Inf,
  aniso_method = "vgm",
  type_joint = "Exp",
  prodsum_k = 0.25,
  surface = FALSE,
  start_vals = c(NA, NA, NA),
  miscFitOptions = list(),
  measurement_error = c(0, 0, 0),
  cores = 1L,
  verbose = TRUE,
  optimizer = "lbfgsb",
  objective = "WLS",
  n_restart = 1L,
  optimizer_control = list()
)
}
\arguments{
\item{formula}{A formula (e.g. \code{PM10 ~ 1}).}

\item{input_data}{Training data (STFDF, STSDF, STIDF, or sftime).}

\item{new_data}{Prediction grid. If missing, generated automatically via
\code{\link{create_new_data.ST}}.}

\item{type_stv}{Character. ST variogram model type. Passed as \code{typestv}
to \code{\link{autofitVariogramST}}.}

\item{block}{Numeric. Block size for block Kriging.}

\item{model}{Character vector. Candidate marginal variogram model families.}

\item{kappa}{Numeric vector. Kappa values for Matern-family models.}

\item{fix.values}{Numeric vector (3). Fixed initial values for nugget,
range, sill.}

\item{newdata_mode}{Character. \code{"rect"} or \code{"chull"}.}

\item{newdata_npoints}{Integer. Number of prediction grid points.}

\item{GLS.model}{Variogram model for GLS sample variogram (rarely needed).}

\item{tlags}{Integer vector. Temporal lags.}

\item{cutoff}{Numeric. Maximum spatial lag.}

\item{width}{Numeric. Spatial lag bin width.}

\item{forward}{Integer. Number of future time steps to predict.}

\item{predict_chunk}{Integer or NULL. Chunk size for large prediction grids.}

\item{nmax}{Numeric. Maximum number of ST neighbours.}

\item{aniso_method}{Character. Anisotropy estimation method.}

\item{type_joint}{Character. Joint variogram component model family.}

\item{prodsum_k}{Numeric. k parameter for productSum models.}

\item{surface}{Logical. Return variogram surface?}

\item{start_vals}{Numeric vector (3). Not used in v2.0 (kept for API compat).}

\item{miscFitOptions}{List. Not used in v2.0 (kept for API compat).}

\item{measurement_error}{Numeric vector (3). Measurement error components.}

\item{cores}{Integer. Cores for variogramST computation.}

\item{verbose}{Logical. Print progress messages.}

\item{optimizer}{Character. \code{"lbfgsb"} or \code{"grid"}.}

\item{objective}{Character. \code{"WLS"} or \code{"MLE"}.}

\item{n_restart}{Integer. Number of optimisation restarts.}

\item{optimizer_control}{List. Extra control arguments for the optimiser.}
}
\value{
An \code{autoKrigeST} object.
}
\description{
Convenience wrapper that fits the variogram and predicts in one call.
Internally calls \code{\link{fitVariogramST}} then
\code{\link{predictKrigeST}}.
}
\examples{
data(air)
deair <- STFDF(stations, dates, data.frame(PM10 = as.vector(air)))
deair_rs <- deair[, 3751:3800]
## Not run:
# akst <- autoKrigeST(formula = PM10 ~ 1, input_data = deair_rs,
#                     cutoff = 300000, width = 30000, tlags = 0:7, cores = 4)
}
