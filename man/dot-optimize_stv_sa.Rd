% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimizer_sa.R
\name{.optimize_stv_sa}
\alias{.optimize_stv_sa}
\title{Simulated Annealing Optimiser for ST Variogram Fitting}
\usage{
.optimize_stv_sa(
  stva_emp,
  model_template,
  bounds,
  maxit = 5000L,
  temp = 10,
  tmax = 10L,
  lbfgsb_refine = TRUE,
  lbfgsb_maxit = 500L
)
}
\arguments{
\item{stva_emp}{Empirical ST variogram (\code{StVariogram}).}

\item{model_template}{Initial \code{vgmST} model (defines type/structure).}

\item{bounds}{List with \code{lower} and \code{upper} named numeric vectors
(from \code{\link{st_param_bounds}}).}

\item{maxit}{Integer. Maximum number of SANN function evaluations.
Default 5000.}

\item{temp}{Numeric. Initial temperature for the SANN cooling schedule.
Default 10.}

\item{tmax}{Integer. Number of function evaluations at each temperature
level before cooling.  Default 10.}

\item{lbfgsb_refine}{Logical. If \code{TRUE} (default), run a final short
L-BFGS-B step from the SANN solution.}

\item{lbfgsb_maxit}{Integer. Maximum L-BFGS-B iterations for the
refinement step.  Default 500.}
}
\value{
Best-fitting \code{vgmST} object found.
}
\description{
Uses \code{stats::optim(method = "SANN")} to globally search the WLS
objective surface for a \code{vgmST} model, then refines the best
solution with a short L-BFGS-B run.  Parameters are transformed to an
unconstrained space via a scaled logistic map so that SANN can explore
freely without explicit boundary enforcement.
}
\seealso{
\code{\link{autofitVariogramST}}, \code{\link{st_param_bounds}}
}
\keyword{internal}
