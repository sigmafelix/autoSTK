% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coerce_st.R
\name{cubble_to_sftime}
\alias{cubble_to_sftime}
\title{Convert a cubble object to an sftime object}
\usage{
cubble_to_sftime(x, time_col = NULL, key_col = NULL)
}
\arguments{
\item{x}{A \code{cubble_df} object (from the \pkg{cubble} package).}

\item{time_col}{Character. Name of the time column in the temporal face.
If \code{NULL} (default), the function tries to detect it automatically
by looking for POSIXct/Date columns.}

\item{key_col}{Character. Name of the site identifier column shared by the
spatial and temporal faces.  Detected automatically from
\code{cubble::key_vars()} when \code{NULL}.}
}
\value{
An \code{sftime} object with a \code{geometry} column (sfc_POINT).
}
\description{
Handles both nested and temporal faces of a \pkg{cubble} object.  The
resulting \code{sftime} has one row per (location × time) observation with
the spatial geometry attached to every row.
}
